# Complete current runner source

This page exposes the exact current direct verifier and independent checker
inline. The fixed entrypoint imports these functions and exits nonzero for
any failed check.

## Direct empirical verifier

```python
{{EMPIRICAL_SOURCE}}
```

## Independent checker

```python
{{INDEPENDENT_CHECKER_SOURCE}}
```

Raw results are reachable from [Reproduce](#/reproduce).
