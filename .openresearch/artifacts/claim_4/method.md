# Claim 4 method

The certificate reconstructs the entire implication chain:

1. the `W_p` convergence characterization proves continuity for every
   continuous `p`-growth integrand;
2. explicit concatenation and outer constructors prove subalgebra closure;
3. regularity and a bounded Urysohn bump separate arbitrary distinct measures;
4. compactness supplies tightness and `p`-uniform-integrability, which bound
   the truncation error;
5. van Nuland's global `C_0` UAT approximates the truncated integrand;
6. a corrected compact outer neighborhood closes the final epsilon budget.

Quantitative regressions include 256 atomic Kantorovich–Rubinstein checks, all
2,415 pairs among 70 denominator-four measures, an exact compact unbounded
tail family, and a separate rational checker.

Destructive controls use an exponential-growth integrand, replace product by
addition, and double both tail allowances.
